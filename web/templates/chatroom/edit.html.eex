<h2>Edit chatroom</h2>

<%= render "form.html", changeset: @changeset,
                        action: chatroom_path(@conn, :update, @chatroom) %>

<%= link "Back", to: chatroom_path(@conn, :index) %>
